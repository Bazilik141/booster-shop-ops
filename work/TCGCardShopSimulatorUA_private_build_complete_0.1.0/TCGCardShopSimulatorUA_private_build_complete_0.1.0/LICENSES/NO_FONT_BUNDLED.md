# Font status

No font is bundled in 0.1.0.

Translator 1.0.3 documents support for custom files named `FredokaOne-Regular.ttf/.otf` and `LiberationSans.ttf/.otf`, but the handoff requires an actual Cyrillic glyph failure to be demonstrated before adding one. Any future font must include a verified redistribution license.
