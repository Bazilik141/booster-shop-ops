# PAY — декомпозиція: mono ПЧ × ПУМБ × статус «Передзамовлення» (2026-07-21)

> ⚠️ **2026-07-27 — розділи 6.1–6.3 СКАСОВАНО.** Технічна модель ПУМБ у розділі 6
> писалась від загальної PDF «v2.4» і виявилась хибною: у реальному протоколі
> банку немає ні HMAC-підпису, ні redirect-флоу, ні підпису в callback.
> Актуальне джерело для PAY-002: `plans/PAY-002_pumb-protocol-revision_20260727.md`.
> Розділи 5, 6.5, 7, 8 (факти договору, рішення власника по флоу, QA, ризики)
> лишаються чинними.

Date: 2026-07-21 | Джерела: `handoffs/handoff_PAY-001_RESET_checkout-architecture-correction_20260721.md` (актуальний стан mono), `diagnostics/PAY-001_progress-and-preorder-followup_report_20260721.md` (progress-звіт Codex), офіційна PDF-документація ПУМБ «Оплата частинами та кредитування» v2.4 (надана власником 2026-07-21), пряма цитата фахівця ПУМБ.

## 1. Висновок

Три окремі задачі замість однієї розмитої. **(А)** PAY-001 mono — sandbox повністю пройдено (4 телефони, регресія чиста), лишились Phase 2 UI + production-реєстрація. **(Б)** ПУМБ — нова задача PAY-002: API справді структурно ~90% схоже на mono (HMAC-SHA256/Base64, create→callback→confirm-на-відправці, ідемпотентність по store_order_id, return/cancel), тому робимо mono до кінця і клонуємо по шаблону; договір з ПУМБ треба запускати ВЖЕ (тестовий контур доступний лише після підписання — це найдовший ланцюжок). **(В)** «Передзамовлення» — НЕ кредитна механіка: власник хоче лише новий статус замовлення в адмінці. Виносимо в окрему маленьку задачу ORD-PREORDER, скоуп PAY-001 Phase 2 НЕ змінюється. Помилкове трактування «пре-замовлення × ПЧ» з progress-звіту Codex (розділ 6) — зняте власником 2026-07-21.

## 2. Тип задачі

Декомпозиція/roadmap-планування. Виконавча частина далі торкається checkout/payment (HIGH-RISK) — кожен патч цих задач іде через власний smoke test.

## 3. Owner

Mixed — Owner (договір ПУМБ, тарифи, бізнес-рішення по кредиту 24 міс і наборах платежів; підтвердження roadmap-записів) + Claude (handoff кожної підзадачі) + Codex (патчі).

## 4. Статус

| Задача | Стан |
|---|---|
| PAY-001 mono — backend + sandbox | ✅ Done (QA 4 телефони 2026-07-21, isolation повернуто, `status=0`) |
| PAY-001 mono — Phase 2 UI (товарна → модалка → checkout/checkout) | ✅ Задеплоєно й пройшло QA2 (2026-07-25, деталі — Notion PAY-001) |
| PAY-001 mono — production | ✅ **Done, 2026-07-27** — власник закрив задачу на підставі реального бойового замовлення (стороння людина, повний флоу, кабінет монобанку + OpenCart, скасовано власником після перевірки). Повний 15-кроковий regression не загублений — перенесено в нову задачу PAY-001-SMOKE (розділ 9), яка запускається один раз для ОБОХ провайдерів після готовності ПУМБ |
| PAY-002 ПУМБ — договір | ✅ **Підписано 2026-07-21** (№ SF1/21.3.2/4510 через «Вчасно», чинний до 31.12.2026, автопролонгація по 1 року). Бізнес-рішення власника закриті: обидва провайдери лише 3/4/5 платежів, лише «Сплата частинами» (споживчий кредит до 24 міс НЕ вмикаємо) |
| PAY-002 ПУМБ — реальний протокол | ✅ **Отримано і розібрано 2026-07-27** (архів «Інтеграція ПУМБ Сплачуй частинами») — `plans/PAY-002_pumb-protocol-revision_20260727.md`. Технічна модель виявилась іншою за структурою, ніж припускалось 21.07 (OAuth2 замість HMAC, без redirect, callback без підпису) |
| PAY-002 ПУМБ — тестовий/продовий callback URL | ✅ Вирішено 2026-07-27 — один хост, два роути (Variant A), деталі `PAY-002_pumb-protocol-revision` §5.1 |
| PAY-002 ПУМБ — технічний доступ | ❌ Чекаємо від банку: логін/пароль OAuth2, IP для нашого allowlist, підтвердження формату callback, відповіді на 11 питань (розділ 7 ревізії) |
| ORD-PREORDER статус «Передзамовлення» | Нова задача, скоуп підтверджений власником |
| **PAY-003 (нова, 2026-07-27)** — спільна проміжна сторінка очікування підтвердження | Not started, деталі — розділ 10 нижче |
| **Консолідація order-статусів** (6 mono → 5 спільних, нейтральні назви) | ✅ **Схвалено власником 2026-07-27**, включно зі злиттям «активна»+«завершена» → «оформлено». Деталі — `PAY-002_pumb-protocol-revision` §8a. Йде разом з Codex-раундом PAY-002 |
| **PAY-001-SMOKE (нова, 2026-07-27)** — фінальний спільний QA-гейт | Not started, заблокована до готовності PAY-002+PAY-003+консолідації статусів. Деталі — розділ 9 нижче, повний план `plans/PAY-001-SMOKE_unified-credit-qa_20260727.md` |
| Roadmap-записи (dashboard/Notion) для PAY-002, PAY-003, PAY-001-SMOKE, ORD-PREORDER | PAY-002, PAY-003, PAY-001-SMOKE заведені в Notion 2026-07-27; ORD-PREORDER досі очікує |

## 5. Наступна дія — логічний порядок

1. ~~Договір ПУМБ~~ ✅ підписано 2026-07-21; ~~бізнес-рішення~~ ✅ закриті: 3/4/5 для обох провайдерів, лише ПЧ.
2. ~~PAY-001 Phase 2 UI~~ ✅ зроблено і пройшло QA2.
3. ~~PAY-001 закриття~~ ✅ власник закрив 2026-07-27 (розділ 8); regression перенесено в PAY-001-SMOKE.
4. **Owner → ПУМБ (менеджер Р. Назаренко):** надіслати два фінальні callback URL (§5.1 ревізії) і 11 уточнюючих питань (розділ 7 ревізії); чекати доступи ІТ банку (орієнтир ~1 тиждень з моменту постановки їм задачі).
5. **Claude→Codex (після отримання доступів):** handoff на скелет `extension/pumb_credit` — OAuth2-клієнт з кешуванням токена, transaction-таблиця, callback-роут (Basic auth, дві версії — прод/тест), мапінг станів, **разом з консолідацією order-статусів** (6→5, `PAY-002_pumb-protocol-revision` §8a) в тому самому раунді.
6. **Паралельно/після початку PAY-002:** PAY-003 — спільна проміжна сторінка (розділ 10). Власник прив'язав це до моменту реалізації ПУМБ, не до "негайно".
7. **Перед будь-яким публічним увімкненням:** PAY-001-SMOKE (розділ 9) — єдиний фінальний прогін на обох провайдерах разом, не окремо.

## 6. Codex handoff — технічний контекст

### 6.1 ПУМБ API проти mono — що збігається (база для клонування)

Підпис `Base64(HMAC-SHA256(body, secret))`; заголовки-пара store-id/signature (у ПУМБ — `X-Store-Id`/`X-Signature`); create → клієнтське підтвердження → callback + polling fallback; **confirm видачі = момент руху грошей** (як у mono — списання першого платежу), викликається вручну при реальній відправці; cancel до confirm / return після; ідемпотентність по `store_order_id` (ПУМБ на дубль явно віддає `409`); суми в копійках; products-масив у заявці. Архітектура mono_chast (transaction-таблиця, стани, callback-верифікація, poll) переноситься майже 1:1.

### 6.2 Ключові відмінності (таблиця для майбутнього PAY-002 патча)

| Параметр | mono ПЧ | ПУМБ |
|---|---|---|
| Флоу клієнта | підтвердження в застосунку mono по телефону | **redirect на `redirect_url`** банку + `return_url` назад |
| Платежі | 3/4/5 (правило власника, `available_parts_count`) | ✅ **Закрито 2026-07-21: теж 3/4/5, лише ПЧ (без CREDIT)** — підтверджено власником після діалогу з менеджером ПУМБ (Р. Назаренко) |
| Мін. сума | 500 грн (правило власника) | **1000 грн** (вимога банку) |
| Макс. сума | — | 100 000 грн ПЧ / 300 000 грн кредит |
| TTL confirm | невідомо (не блокує: НП-відправка = дні) | **7 днів для інтернет-магазинів** (менеджер ПУМБ, 2026-07-21; загальна PDF-док каже 30 — для e-com банк ставить 7), далі автоскасування; менеджер може «відновити заявку» вручну |
| Статуси | 6 станів mono | 10 станів — мапити на ті самі 6 OC-статусів + окремо CLIENT_PROCESSING/CANCELED |
| Callback | підпис у заголовку | підпис у тілі; **відповісти 200 за 10 с**, 5 ретраїв по 15 хв; IP-whitelist |
| Sandbox | публічний, без договору | **лише після договору**; тест-телефони +38000000000{1,2,3} |
| Rate limit | є (докс mono) | 100 req/хв (`429`) |
| Комісії | 3→2.9%, 4→4.1%, 5→5.9% (підтверджено) | ✅ **З Додатку №2 договору: NEW_3→3.00%, NEW_4→4.50%, NEW_5→5.80%** (сітка йде до NEW_24→19%, але вони поза скоупом). Банк утримує комісію сам із перерахування |

### 6.3 Що це означає для коду

- Модалка вибору провайдера (вже в дизайні PAY-001-UI) стає єдиною точкою входу для обох: картка mono (активна після Phase 2) + картка ПУМБ (активується в PAY-002). Фільтр доступності провайдера по сумі кошика: mono ≥500, ПУМБ ≥1000 і ≤ліміт.
- ПУМБ — окремий extension (`extension/pumb_credit` або аналог) зі своєю transaction-таблицею і своїм `payment_pumb_*_status`-прапорцем; НЕ розширювати mono_chast всередині.
- Callback URL: цитата фахівця ПУМБ — «потрібна інфо з розділу кредити, вкладка пумб, callback url і все». Callback-роут з'явиться з extension-скелетом PAY-002; URL можна зарезервувати і повідомити ПУМБ заздалегідь (формат: `?route=extension/pumb_credit/payment/pumb_credit.callback` — фінальний вигляд у handoff PAY-002).
- NCRM: окрема майбутня міграція `credit_pumb_*` payment types (за зразком 0013), лише коли відомі фінальні набори платежів і комісії.

### 6.4 ORD-PREORDER (окремо, поза PAY)

Скоуп: додати order status «Передзамовлення» в OpenCart адмінку (`oc_order_status`, uk-локаль) + узгодити мапінг у NCRM order-sync (щоб новий статус не впав у «невідомий»). Це НЕ кредитна логіка і НЕ чекаут — ризик низький, але order-sync уже має статусні мапінги, тому патч має пройти перевірку проти `ncrm/supabase/functions/order-sync/index.ts`. TTL-питання з progress-звіту Codex знімається як блокер: для ПУМБ відповідь у доксах (30 днів), для mono — цікаво лише перед production, не для передзамовлень.

### 6.5 Факти з договору ПУМБ (№ SF1/21.3.2/4510, 2026-07-21), що впливають на реалізацію і процеси

- **Оформлення кредиту — клієнт самостійно в мобільному додатку PUMB Online** (п.1.1, 1.3); магазин лише передає контактні дані покупця через API. Узгодити з redirect_url-флоу з API-доки при отриманні «Порядку обміну» — можливо, redirect веде саме в app-флоу.
- **Гроші:** банк перераховує кошти наступного робочого дня після передачі товару покупцю (свята/вихідні — до 2 роб. днів), комісію утримує сам із суми перерахування (п.2.1.3, 2.2.9). Для NCRM: сума надходження = ціна мінус комісія, окремий payment_type `credit_pumb_3/4/5`.
- **Рішення банку по заявці — не пізніше наступного робочого дня**; на цей час магазин зобов'язаний **резервувати товар** (п.2.2.2). Стик з логікою резервів/стоку — врахувати в handoff PAY-002.
- **Видача товару — в день отримання рішення** банку + повідомити банк через API про передачу (п.2.2.3) — це аналог confirm у mono.
- **Обраний флоу відвантаження (власник, 2026-07-21, уточнено — «комбінований» на базі варіанту 1):** сигнал «відвантаження товару» йде банку **в момент фізичної передачі посилки поштовому оператору** (завантаження у поштомат/відділення, створена ТТН), не в момент оформлення. Це вкладається в TTL 7 днів і трохи звужує вікно невикупу. Заявка переходить у `FOUNDED` (статусу нема в PDF v2.4 — чекати актуальний «Порядок обміну»), гроші на рахунку за 5–10 хв.
- **Автоматизація в OpenCart (проєктне рішення для PAY-002):** обидва напрями автоматичні. Вхідний: callback банку (клієнт підтвердив у застосунку, схвалення тощо) → модуль сам оновлює статус замовлення в адмінці — власник «бачить це» без ручних дій. Вихідний: власник міняє статус замовлення на «Відправлено» → event-hook на зміну статусу сам викликає API «відвантаження товару». Вимоги: ідемпотентність через transaction-таблицю (без подвійного confirm при повторній зміні статусу); якщо виклик API впав — явна помилка/алерт власнику, НЕ мовчазний фейл; сумісність з існуючим NCRM order-sync хуком на зміни замовлень (не конфліктувати). Той самий hook-патерн придатний і для mono confirm — уніфікувати в Phase 2/PAY-002.
- **Перший платіж клієнта у ПУМБ — через місяць після видачі** (слова власника з розмови з банком), не в момент confirm (відмінність від mono, де confirm одразу списує перший платіж). Наслідок для невикупу: клієнт на момент повернення ще нічого не платив → банку повертається повна сума; операційно це «майже звичайне повернення» + API-статуси банку (return по API, свій чек повернення в Checkbox — як зазвичай).
- Фіскалізація: власник фіскалізує чек у день замовлення (Checkbox), як і зараз — менеджер банку на це питання прямо не відповів; на код не впливає, у разі сумнівів власник уточнює у свого бухгалтера.
- **Повернення:** за заявою покупця — перерахувати банку вартість товару (мінус перший внесок клієнта) протягом **3 робочих днів** (п.2.2.4).
- **Рахунок-фактура з підписом і печаткою** покупцю на товар у кредит (п.2.2.2) — операційний процес власника, не код.
- **Реклама:** будь-які рекламні матеріали про купівлю в кредит ПУМБ **погоджуються з банком до публікації** (п.2.2.8) — стосується і UI (бейджі/модалка/банери з брендом ПУМБ). Макети надсилати на погодження до релізу PAY-002 UI.
- **Конфіденційність (п.2.2.7):** зміст договору і креденшли не розголошуються. **PDF договору в git-репо НЕ комітити**; у репо тримаємо лише мінімально потрібні робочі факти (цей розділ).
- Строк дії: до 31.12.2026, автопролонгація. Email банку: pumb.partnerhelp@fuib.com, документообіг — «Вчасно».

## 7. QA checklist

- PAY-001 Phase 2 UI: чекліст буде в handoff патча + обов'язковий `bs-checkout-smoke` (11 кроків) перед будь-яким увімкненням.
- PAY-002: повторити структуру sandbox-QA mono (створення/callback/poll/fail/confirm/невірний підпис/дубль `store_order_id`/мін-макс суми/регресія Hutko-COD-IBAN) на тест-телефонах ПУМБ + окремо: redirect-флоу туди-назад, callback за 10 с, автоскасування по TTL (документально, не чекати 30 днів).
- ORD-PREORDER: створити тестове замовлення → виставити статус → перевірити відображення в адмінці та коректний прохід через order-sync у NCRM.

## 8. PAY-001 — закрито (2026-07-27)

Власник повідомив 2026-07-27: «Впровадження оплати в кредит від моно завершено.
Тестове замовлення пройшло успішно» — і уточнив далі: це було **реальне бойове
замовлення від сторонньої людини** (не власника), воно коректно з'явилося і в
кабінеті монобанку, і в OpenCart, після чого власник сам його скасував,
переконавшись, що механізм працює.

Це істотно сильніший доказ, ніж зафіксований в останньому закоміченому стані
дашборда: там значився лише sandbox-смок і два відкриті гейти — (1) повний
15-кроковий smoke не перепройдено після Phase 2d-фіксів, (2) production
вважався заблокованим, бо монобанк видає `point_id` лише після власної
перевірки інтеграції. Той факт, що бойове замовлення реально дійшло до
кабінету монобанку, означає, що `point_id` уже видано і працює — гейт (2)
знятий фактом.

**Власник підтвердив 2026-07-27: закрити зараз.** Notion `PAY-001` →
`Done`. Гейт (1) (повний 15-кроковий regression) не втрачається — він
переноситься в нову задачу PAY-001-SMOKE (розділ 9), яка свідомо відкладена
до готовності ПУМБ: замість повторного mono-only прогону зараз і ще одного
after PUMB, власник тестує готовий спільний продукт один раз.

## 9. PAY-001-SMOKE (нова задача, 2026-07-27) — фінальний спільний QA-гейт

**Джерело:** рішення власника в чаті 2026-07-27, одразу після закриття PAY-001.
**Повний план:** `plans/PAY-001-SMOKE_unified-credit-qa_20260727.md`.

**Що це:** останній регресійний гейт перед тим, як «Покупка в кредит» (mono +
ПУМБ) стає публічно доступною клієнтам без нагляду власника. Не замінює
sandbox-QA кожного окремого провайдера під час розробки — це фінальна спільна
перевірка вже зібраного продукту.

**Чому не зараз:** запускати повний 15-кроковий смок mono-only зараз, а потім
ще раз повний смок після ПУМБ — подвійна робота на застарілому наполовину
продукті. Задача явно заблокована до готовності: PAY-002 (`extension/pumb_credit`
з робочим тест-контуром), PAY-003 (спільна проміжна сторінка), консолідації
order-статусів (розділ вище, `PAY-002_pumb-protocol-revision` §8a).

**Структура плану** (`PAY-001-SMOKE_unified-credit-qa_20260727.md`):
Stage 0 — базовий 11-крокий `bs-checkout-smoke` regression (не кредитний, floor
перед кредитними стадіями); Stage 1 — повний mono-флоу (переперевірка, не
інкрементальна); Stage 2 — повний ПУМБ-флоу (create/callback/poll-fallback/
shipment/refund/невалідний callback); Stage 3 — спільний UI і консолідація
статусів (адмінський dropdown, відновлення PAY-003 після закритої вкладки,
спільна checkout success, NCRM-мапінг); Stage 4 — регресія Hutko/COD/IBAN.

**Закриття задачі:** після проходження всіх стадій (або явної відмови власника
від конкретного кроку з причиною) — це і є момент, коли «Покупка в кредит»
дозволяється зробити публічно видимою обом провайдерам.

## 10. PAY-003 (нова задача, 2026-07-27) — спільна проміжна сторінка очікування підтвердження

**Джерело:** рішення власника в чаті 2026-07-27, під час обговорення PAY-002.

**Що це:** проста сторінка в стилі магазину (дизайн серії RD-XX), яка
відкривається одразу після оформлення замовлення на чекауті (клік «Оформити») і
тримає клієнта, поки він не підтвердить оформлення кредиту у своєму застосунку
— mono push-підтвердження або підпис у застосунку PUMB Online. Лише після
підтвердження клієнт потрапляє на звичайну `checkout success`.

**Спільна для обох провайдерів**, тому що «Покупка в кредит» — один вид
покупки (підтверджено в PAY-001: mono і ПУМБ — паралельні картки в одній
модалці, не окремі способи оплати в звичайному чекауті). Звідси й `checkout
success` після підтвердження — теж одна й та сама сторінка для обох банків;
власник підтвердив це припущення 2026-07-27.

**Owner:** Claude Design (макет у стилі RD-XX) + Codex (інтеграція зі станами
транзакції обох extension: `ocp5_mono_chast_transaction.state` /
`ocp5_pumb_credit_transaction.state`).

**Послідовність:** власник прив'язав цю задачу до моменту реалізації ПУМБ («коли
реалізуємо ПУМБ, будемо полірувати чекаут саксес») — не до негайного старту.
Логічно: після готовності `extension/pumb_credit` (PAY-002 технічна частина),
разом з полішингом checkout success, перед публічним увімкненням ПУМБ клієнтам.

**Технічна прив'язка:** сторінка читає стан транзакції (poll або
push-оновлення після callback) і показує проміжні статуси клієнту людською
мовою — «очікуємо підтвердження», «схвалено», «відхилено» тощо. Не плутати з
адмінськими OC order-статусами (`PAY-002_pumb-protocol-revision` §8а) — це фронтова сторінка над тим самим
станом, без нових order-статусів.

**Ризики:** якщо клієнт закриє вкладку до підтвердження — сторінка має
коректно відновлюватись при поверненні (за замовленням/сесією), а не губити
стан; polling-інтервал не повинен створювати зайве навантаження при довгому
очікуванні (TTL 7 днів у ПУМБ — сторінка не може вічно тримати активний polling,
потрібен розумний бекоф або email/сповіщення замість вічного polling).

## 11. Ризики

- **Checkout/payment — HIGH-RISK зона:** кожен патч PAY-серії торкається живого флоу оплати; правила з RESET-handoff лишаються (status-прапорці, isolation, координація з ST-2c cutover, smoke test обов'язковий).
- **PDF v2.4 ≠ реальне API** (статус `FOUNDED`, TTL 7 днів замість 30 вже розійшлись з доками) — PAY-002 handoff писати ТІЛЬКИ від актуального «Порядку обміну» від банку, PDF v2.4 вважати орієнтиром, не специфікацією.
- **Варіант 1 (гроші в день відправки) переносить ризик невикупу на нас:** клієнт не забрав посилку → повернення через API + перерахування коштів банку за 3 роб. дні (п.2.2.4, мінус перший внесок клієнта). Потрібен операційний процес трекінгу невикуплених ПЧ-посилок (НП статуси) — інакше касовий розрив.
- **Реклама без погодження макетів = договірний ризик** (п.2.2.8, банк може перекласти штрафи на продавця) — жодного публічного UI/банера з брендом ПУМБ до письмового погодження макетів. Робоче тлумачення: погоджується шаблон, не кожна товарна сторінка — але це підтвердити письмово (розділ 5.2в).
- **Резервування товару + видача в день рішення банку** — операційні зобов'язання договору; якщо процес власника їх не витягує (наприклад, товар закінчився за час рішення), потрібен cancel-флоу в модулі.
- **Callback-вимоги ПУМБ жорсткіші** (200 за 10 с, IP-whitelist): якщо хостинг повільний на cold-start — ризик пропущених статусів; poll-fallback обов'язковий як у mono.
- **ORD-PREORDER зачіпає order-sync мапінги** — новий статус без мапінгу може зламати синк замовлень у NCRM; патч тільки разом з перевіркою index.ts.
- «90% схожості» — оцінка ПУМБ, не наша: не копіювати код сліпо, кожну відмінність з таблиці 6.2 проганяти явно.
