# Codex Report — RD-13.1A: guest NP payload sync

Date: 2026-07-11

## Scope

Handoff task A only. The patch changes `checkout.twig` client code so all duplicate inputs associated with `#form-register` receive the same NP city/address values before `register.save` serializes the form. Server validation is unchanged.

## Files touched

```
patches/RD-13.1A_guest_np_payload_sync_20260711.php
```

Runtime target: `catalog/view/template/checkout/checkout.twig`.

## Dry-run result

```
[RD-13.1A] dry_run=ok
[RD-13.1A] would_change=catalog/view/template/checkout/checkout.twig
```

## php -l result

```
No syntax errors detected in RD-13.1A_guest_np_payload_sync_20260711.php
```

## Idempotency

After an isolated apply, a repeat run returned `already_applied=yes`.

## Rollback

The runner backs up the original file at `_patch_backups/RD-13.1A_guest_np_payload_sync_20260711_<timestamp>/catalog/view/template/checkout/checkout.twig` before writing. Restore that file to `catalog/view/template/checkout/checkout.twig`.

## Run command (owner)

```bash
cd ~/public_html || exit
php RD-13.1A_guest_np_payload_sync_20260711.php && php -r 'require "config.php"; foreach (glob(DIR_CACHE . "cache.*") ?: [] as $f) if (is_file($f)) @unlink($f); foreach (glob(DIR_CACHE . "template/*") ?: [] as $f) if (is_file($f)) @unlink($f); echo "cache cleared\n";'
```

## Post-deploy QA checklist

- [ ] Guest: choose an NP city and warehouse, then inspect `checkout/register.save` in Network: `shipping_address_1` and `shipping_city` are populated.
- [ ] Guest: blur an NP field and confirm no address/city validation error appears.
- [ ] Complete one test checkout; confirm no extra `confirm.confirm` request occurs before the trusted place-order click.

## Side effects / risks

Checkout/NP path, medium risk. The patch has no DB changes and does not alter server validation, shipping quotes, payment logic, or order creation; it only makes duplicate client form fields consistent before serialization. Local live-source simulation passed; production Network proof remains owner QA.
