# Codex Report — TECH-012: legacy product 404 → 301

Date: 2026-07-02

## Scope
Implemented the handoff as an additive `.htaccess` redirect block. Ten legacy
product URLs were mapped to current canonical product URLs after backup DB,
sitemap, and live HTTP verification. No DB, sitemap, robots, controller,
checkout, payment, or product-data changes.

The `Pokemon-boosters-mix-lowpull` mapping is included because the current
backup identifies active product 73 as the same Outlet Mix product with
canonical `Pokemon-Japanese-outlet-booster`; live checks returned old 404 and
target 200.

## Files touched
```
patches/TECH-012_legacy404-301_20260702.php — self-contained .htaccess patch
```

Runtime target:
```
.htaccess
```

## Verification basis
- Backup: `backup-7.1.2026_10-11-00_boosters.tar.gz`
- Backup `.htaccess`: HTTPS/www redirect and OpenCart catch-all anchors found once
- Backup DB + sitemap: all ten current product mappings verified
- Live preflight on 2026-07-02: all ten old URLs returned 404; all ten targets returned 200

## Dry-run result
```text
php_lint=ok
file_preflight=ok:.htaccess
assert=transformed_state:ok
would_change=yes:.htaccess
done=ok
```

## php -l result
```text
No syntax errors detected in TECH-012_legacy404-301_20260702.php
```

The target is Apache configuration, not PHP. The patch additionally performs
exact anchor counts, exact block assertions, and ordering checks before and
after writing.

## Idempotency
Re-running against an already patched fixture returns:
`already_applied=yes`, `changed=none`, `done=ok`.

## Rollback
Backup at:
`_patch_backups/TECH-012_legacy404-301_20260702-<timestamp>/.htaccess`

To restore:
```bash
cp _patch_backups/TECH-012_legacy404-301_20260702-<timestamp>/.htaccess .htaccess
```

The patch automatically restores this backup if a post-write assertion fails.

## Run command (owner)
```bash
cd ~/public_html || exit
php TECH-012_legacy404-301_20260702.php
```

Cache clearing is not required for `.htaccess`.

## Deployment result
Owner deployed the patch on 2026-07-02 at `17:37:01+00:00`.

```text
backup=_patch_backups/TECH-012_legacy404-301_20260702-20260702-173701/.htaccess
changed=.htaccess
assert=final_state:ok
done=ok
```

Post-deploy live readback:
- all ten legacy URLs return one exact `301` to their mapped canonical;
- all ten canonical targets return `200`;
- home, `/product/Pokemon-boosters-Ninja-Spinner`, and `/catalog/Pokemon` return `200`.

## Post-deploy QA checklist
- [x] Patch output contains `done=ok`
- [x] Each old URL returns one 301 hop to its specified current product
- [x] Each target returns 200
- [x] Home, one current product, and `/catalog/Pokemon` return 200
- [x] Runtime write was limited to the validated commented redirect block
- [x] No redirect chain or loop

## Side effects / risks
High SEO risk because this changes redirect behavior. The change is limited to
one additive block after the HTTPS/www canonical redirect and before the
OpenCart catch-all. It does not touch DB or application code.
