# Owner QA — one pass closing 3D-P-013, 3D-P-015 and 3D-P-024

Date: 2026-08-09 · For: the owner, PC only · Author: Claude (chat)

All three tasks are substantively finished and have been waiting on the same short desktop QA. The
checks overlap heavily, so this is one pass, not three.

**Where:** open the canonical dashboard and press `Ctrl+F5` first.
`file:///C:/Users/14bez/Downloads/Booster%20Shop/booster-shop-ops/dashboard/booster-dashboard.html`
→ tab **3D-друк**.

Test SKU: `ACC-3D-DITTO-410`. Do not use `FIG-LUFFY-500` — it still carries the deliberate test value
`Ціна під викуп = 999 грн`.

There is no deployment step for the dashboard. `Ctrl+F5` is the whole thing.

---

## A. Persistence of Q/R/S — closes 3D-P-013 and 3D-P-015 together

Both tasks ask the same question: does a value written from the dashboard actually land in
`Номенклатура` and survive a reload, rather than living in the browser?

Zone **Вироби**, select `ACC-3D-DITTO-410`:

1. Change **РРЦ фактична** to a distinct test number — say `101`.
2. Change **Ціна під викуп** to `77`.
3. Paste anything recognisable into **Посилання на модель**.
4. Save.
5. Press `Ctrl+F5`.
6. Re-open the same SKU.

- [ ] All three values are still there after the reload.
- [ ] Set **РРЦ фактична** back to `100` when done — `100` is the confirmed correct price and the CRM
      side now agrees with it.

## B. Margin label — closes 3D-P-015

Zone **Інформація**:

- [ ] The margin figure is labelled **«до split»**. It shows the owner's share before the 50/50 split,
      by design — the label is what makes that honest rather than misleading.

## C. Batch calculator — closes 3D-P-024

Zone **Калькулятор**, same SKU:

1. Enter **Сумарний час партії, год** as `1:39` — with the colon, not `1.65`.
2. Enter **Сумарна вага партії, г**, **Вага котушки, г**, **Ціна котушки, грн** with any sane numbers.
3. Set quantity and save the draft.

- [ ] `1:39` is accepted and stored as `1.65` — one hour thirty-nine minutes as a decimal.
- [ ] Per-unit values are written back and the confirmation names `3dp_get_row`.
- [ ] An out-of-range time produces a warning but does not block the save.

## D. Do not test

- **Tablet or mobile widths.** This dashboard is a PC-only local file; mobile belongs to NCRM after
  the migration. The old responsive requirement was withdrawn by the owner on 2026-08-09.
- **Serhiy's local server.** See below.

---

## Owner decision needed — the Serhiy-server items

`3D-P-015` and `3D-P-024` both list Serhiy-local-server QA as a condition of `Done`:

- `3D-P-015`: "Serhiy local-server QA confirming no pricing controls and Serhiy-token write rejection
  for Q/R/S";
- `3D-P-024`: "owner QA of ... Serhiy's two time fields".

**Neither can be satisfied for a long time.** The server has never been installed or tested with two
parties, and on 2026-08-07 the owner re-sequenced `3D-P-007` so that it is built **last** — after the
workbook, the CRM backend and this very dashboard QA all pass.

So those two conditions are checks on a component that belongs to `3D-P-007`, not on the price model
or the time parser. Keeping two `High` tasks open for months over a system that does not exist yet is
how a roadmap stops being believable.

**Recommendation:** close `3D-P-015` and `3D-P-024` on the dashboard and sheet evidence, and move the
Serhiy-token and Serhiy-field verification into `3D-P-007` as explicit acceptance criteria there — so
nothing is lost, it just sits with the task that will actually build the thing.

The owner decides. Nothing here is closed without that word.

## Already resolved — stale blocker text to clean up

Both `3D-P-015` and `3D-P-024` still say the owner must supply published Web App version numbers and
fresh `Code.gs` exports because `SOURCE_STATE.md` records the deployed version as UNKNOWN.

**That is no longer true.** As of 2026-08-09 `crm/apps-script/SOURCE_STATE.md` records main CRM
**V98** and 3D-P **V10**, both verified content-level against the owner's own exports. That clause
should be struck from both blockers when their status is updated.

## After QA

Report which boxes pass. Claude writes the Notion status and the `ROADMAP_FLOW` mirror; the owner
authorises the closure.
