# Хендоф — RD-11: сторінка кошика

Дата: 2026-09-21
Executor: Codex · model=Terra · effort=medium-high — *власник призначив Codex явно; обґрунтування: добре обмежений патч (файли з evidence-бандла вже ідентифіковані, посилання нижче), без архітектурної невизначеності.*

---

## 1. Task ID
`RD-11` (Notion `3706bf20-bdb4-81a4-b3fa-f35e7610defa`)

## 2. Context
`checkout/cart.twig` несе лише 8 `bs-` класів — практично не торкнута редизайном, близька до стокового OpenCart. `cart_list.twig` (партіал рядків) — 23 класи. Блокер у роадмапі («чекати R-13.5») застарілий: `R-13.5` закрито 2026-09-08. Дизайн-джерело: клікабельний прототип `RD-11 RD-12 - кошик, міні-кошик, тост.html`, компонент `CartPage`/`CartRow`/`SummaryCard`/`CartStickyBar` (файл `rd11-cart-page.jsx`), сценарій «Сторінка кошика» в контролах прототипу. Письмова специфікація — блок «RD-11 · Сторінка кошика» у тому ж файлі (`rd-cart-spec-notes.jsx`).

Деплой прямий у продакшн, staging немає. Бекап-джерело evidence-файлів (`backup-9.7.2026_20-35-02_boosters.tar.gz`) — 09.07. **Перевірено 2026-09-21:** жоден із файлів у скоупі цього патчу (`checkout/cart.twig`, `checkout/cart_list.twig`, `checkout/cart.php`, `extension/PintaNovaPoshtaCod/catalog/model/shipping/pinta_nova_poshta.php`) не мав жодного патча між 09.07 і сьогодні — останній патч, що чіпав Pinta-модель доставки, `PAY-002_np-quote-cache_20260831`, увійшов у 09.07-знімок. Нового бекапу не потрібно для старту; стандартна конвенція патча (backup + перевірка anchor-збігів перед записом) — достатній запобіжник на випадок непередбаченого дрейфу.

## 3. Goal
Сторінка кошика виглядає й поводиться як решта редизайну: картки замість сирих таблиць, степер 44×44, липкий підсумок на десктопі й липка CTA-панель на мобайлі, правило безкоштовної доставки, коректний порожній стан. Ціни й логіка чекауту не змінюються.

## 4. What to change
Тільки розмітка й CSS у `checkout/cart.twig` + `checkout/cart_list.twig`.

**Layout**
- Десктоп: grid `minmax(0,1fr) 340px`, gap 24px; права колонка — картка підсумку, `position:sticky; top:16px`.
- Мобайл: одна колонка; картка підсумку під списком **плюс** липка нижня панель (сума + кнопка «Оформити», висота 48px, `box-shadow: 0 -6px 20px rgba(17,24,39,.07)`, `border-top: 1px solid var(--bs-line)`).

**Рядок товару**
- Мініатюра 96×96 (моб. 72×72), назва до 2 рядків (`-webkit-line-clamp:2`), ціна за штуку окремим рядком, степер, «Видалити» текстовим лінком з іконкою корзини.
- Роздільник між рядками — `1px solid var(--bs-line-2)`.
- Колонку «Модель» **прибрати з розмітки**, не ховати `display:none` через скрипт (поточна реалізація ховає скриптом — це і є частина патча).

**Степер кількості**
- Кнопки `−` / `+` по 44×44px (тап-таргет), значення між ними, рамка `1px solid var(--bs-line)`, радіус `var(--bs-r-sm)`, фон `#fff`.

**Картка підсумку**
- «Товари (N)», «Доставка — за тарифами перевізника», роздільник, «До сплати» = сума товарів (без урахування вартості доставки).
- Рядок доставки (див. нижче), CTA «Оформити» (`--bs-green`, h48, 100% ширини), під нею «Продовжити покупки» (біла кнопка, рамка й текст `var(--bs-blue)` — **не** `#1f95d1`, як зараз).

**Правило безкоштовної доставки**
- Текст «Безкоштовна доставка Новою поштою від {{ shipping_pinta_nova_poshta_free_from }}» показується завжди; сума — змінна з адмінки, **не хардкодити** в копірайті.
- Прогрес-бар з’являється тільки коли залишок до порога ≤ 30% від самого порога (рахувати на бекенді/у twig при рендері: `remaining <= threshold * 0.3`; нового налаштування адмінки не додавати).
- Коли поріг досягнуто: текст «Доставка Новою поштою — безкоштовно», колір `var(--bs-green-hover)` (не `--bs-green` — контраст 3.1:1 на світлому фоні недостатній), фон блоку `var(--bs-green-soft)` (новий токен, див. §RD-12-Handoff «Do not touch» нижче — токен додає той самий патч, що й міні-кошик; якщо RD-12 патч ще не деплоєний, додати токен тут).

**Дозволений виняток — дані для правила доставки (`catalog/controller/checkout/cart.php`, метод `getList()`)**
> Рев'ю Кодекса 2026-09-21 виявило реальну проблему: контролер зараз НЕ передає в twig ні поріг безкоштовної доставки, ні сирий (нефарматований) subtotal — є лише масив `$data['totals']` з уже відформатованими рядками. Без цього рядок доставки й прогрес-бар неможливо зробити без хардкоду. Нижче — точковий, перевірений на живому коді виняток із «Do not touch» (§5).

Дозволено додати РІВНО два read-only присвоєння в `getList()`, біля існуючого циклу `$data['totals']` (рядки ~176–189 у знімку):
- `shipping_pinta_nova_poshta_free_from` — **підтверджений реальний ключ налаштування** (звірено з `extension/PintaNovaPoshtaCod/catalog/model/shipping/pinta_nova_poshta.php:221` і `admin/controller/shipping/pinta_nova_poshta.php:177-178,470` — жива дефолтна логіка: `is_numeric($value) && $value > 0 ? (float)$value : 2000.0`). Продублювати рівно цю саму перевірку в `cart.php`, а не вигадувати нову — щоб кошик і чекаут завжди узгоджувались.
- Сирий `sub_total` — узяти з уже наявного циклу `foreach ($totals as $result)`, відфільтрувавши `$result['code'] === 'sub_total'` і взявши його `$result['value']` (float, до форматування валюти). **Без валютної конвертації, без урахування купона.**

**Явно ЗАБОРОНЕНО:** викликати або дублювати `getBoosterCartTotalUah()` / `getBoosterFreeShippingFromUah()` з `PintaNovaPoshtaCod`-моделі — вони враховують купон і конвертацію валют і призначені лише для чекауту. Це вже двічі ламалось у проді (`ST-2c_coupon_shipping_threshold_refresh`, `ST-2c_minicart_shipping_threshold_alignment` — обидва з окремим рев'ю через баги саме на стику купон+поріг). Рядок доставки в кошику — **орієнтовний, до застосування купона**, не гарантія, ідентична чекауту; жодних інших змін логіки/сум.

Ніяких інших правок у `cart.php` — жодних нових методів, жодних змін існуючого циклу `$data['totals']`, жодних змін до `add()`/`edit()`/`remove()`.

**Помилка складу**
- Плашка `--bs-warning-bg` / `--bs-warning-line` над списком товарів (як зараз рендериться `pay002_cart_stock_warning`/`pay002_cart_minimum_checkout_blocked` — зберегти цю логіку, тільки перестилізувати).
- CTA «Оформити» в цьому стані — сірий disabled-вигляд: фон `var(--bs-line-2)`, текст `var(--bs-ink-2)`, рамка `var(--bs-line)`, `cursor:not-allowed`, підпис «Виправте кількість товарів». **Не** напівпрозорий зелений — контраст падає нижче 2:1.

**Порожній кошик**
- Компонент `.bs-empty` (той самий, що вже використовується в `else`-гілці `cart_list.twig` — не переписувати з нуля, лише узгодити відступи з новим макетом), зелена CTA «Перейти до каталогу».

**Рекомендації («Часто беруть разом»)**
- Підключити наявний модуль bestseller (`extension/opencart/catalog/{model,controller,view/template}/module/bestseller.php|twig`) **як є**, без додаткового полірування — концепція рекомендацій зміниться в майбутній задачі. Перед реалізацією підтвердити, чи модуль уже увімкнений десь на сайті; якщо ні — увімкнути мінімально, лише для відображення в кошику.
- 4 картки, на десктопі grid `repeat(4,1fr)`, на мобайлі горизонтальний скрол.

## 5. Do not touch
- `sitemap.xml`, `robots.txt`, редиректи, canonical, `.htaccess`
- Чекаут (`checkout/checkout.twig`, RD-13), оплата, фіскалізація, логіка Нової пошти
- Ціни, розрахунок сум, будь-яка PHP-логіка в `catalog/controller/checkout/cart.php` — **окрім рівно двох read-only присвоєнь, дозволених вище («Дозволений виняток»)**
- Merchant-фід, `Product` schema
- Мінікошик (`common/cart.twig`) і тост (`#alert`) — окремий патч RD-12, не змішувати

## 6. Likely files / areas
Підтверджено з evidence-бандла (`uploads/20260921_cart-minicart-toast-redesign/`):
- `catalog/view/template/checkout/cart.twig`
- `catalog/view/template/checkout/cart_list.twig`
- `catalog/controller/checkout/cart.php` — див. «Дозволений виняток» у §4: рівно два read-only присвоєння в `getList()` (поріг доставки + сирий sub_total), нічого більше
- `boostershop-ds.css` — додати `--bs-green-soft: #F3FBF6` поруч із `--bs-blue-soft`/`--bs-gold-soft`, якщо ще не додано патчем RD-12
- Точна назва bestseller-модуля — виконавець зобов’язаний звірити з реальним деревом проєкту, шлях вище орієнтовний.

## 7. Acceptance criteria
1. `checkout/cart.twig` рендерить картки замість сирої таблиці; на десктопі ≥1024px видно дві колонки, підсумок липкий при скролі.
2. Степер має тап-таргет ≥44×44px (DevTools box model).
3. Правило доставки показує суму з `shipping_pinta_nova_poshta_free_from`, без хардкоду ₴-числа в шаблоні.
4. Прогрес-бар з’являється лише коли `(threshold - subtotal) / threshold <= 0.3`; перевірити на прикладі з різницею вище і нижче межі.
5. Disabled CTA при помилці складу — сірий, текст читається (контраст ≥4.5:1), клік не відправляє форму.
6. Порожній кошик показує `.bs-empty` з CTA на каталог.
7. Мобільна липка панель не перекриває останній рядок товару (padding-bottom контенту враховує її висоту).
8. Консоль без нових помилок; жодних змін у сумах чи URL чекауту.

## 8. QA / smoke test
- 1 товар, кілька товарів, порожній кошик, товар з помилкою кількості — мобайл (390px) і десктоп (1280px+).
- Сценарій «майже досягнуто порогу» і «поріг перевищено» — прогрес-бар з’являється/зникає коректно.
- Рекомендації: клік «+ У кошик» на картці рекомендації додає товар і оновлює суму без перезавантаження сторінки.
- SEO/схема на кошику зазвичай відсутня — якщо виявиться `Product`-розмітка на рядках, зупинитись і звірити з `bs-merchant-schema-qa`.
- Чекаут не зачіпається — `bs-checkout-smoke` не потрібен.

## 9. Rollback note
Бекап змінених файлів у `_patch_backups/RD-11_cart-page_20260921-<timestamp>/` перед записом.
Відкат: залити бекап поверх, скинути кеш теми OpenCart, `Ctrl+F5` на `/index.php?route=checkout/cart`.
Тригер відкату: кнопка «Оформити» не веде на чекаут, суми на кошику розходяться з мінікошиком, зникла можливість видалити товар.

## 10. Recommended status after execution
`In review` — власник перевіряє на продакшні за пп. 7–8. У `Done` — після підтвердження пп. 3–5 (правило доставки і disabled-стан).

---

**Доставка патча:** один файл у `patches/` (тільки RD-11, без RD-12/тосту — окремий робочий пакет). Власник завантажує в `~/public_html` і запускає `php <patch>.php`. Виконавець нічого не комітить, не пушить і не деплоїть.

**Приклади для написання патчу:**
- Клікабельний прототип: `RD-11 RD-12 - кошик, міні-кошик, тост.html` (перемикачі «Сторінка кошика», сценарії вмісту кошика, «Помилка складу в кошику»)
- Компоненти-джерело: `rd11-cart-page.jsx` (розмітка/стилі один-в-один переносяться в twig+CSS), `rd-cart-shared.jsx` (`ShippingInfo`, `Stepper`, `EmptyState` — логіка порога і степера)
- Письмова специфікація: `rd-cart-spec-notes.jsx`, блок «RD-11 · Сторінка кошика»
- Токени: `boostershop-ds.css` (кольори, радіуси, тіні, spacing-крок 4px — нових значень не вигадувати)
- Evidence-розмітка поточного стану: `uploads/20260921_cart-minicart-toast-redesign/catalog/view/template/checkout/cart.twig` і `cart_list.twig`

> **Примітка Claude (chat), 2026-09-21 — review:** повний прототип-пакет (6 `.jsx`-файлів, `tokens.css`, HTML-шелл) додано до репозиторію: `handoffs/RD-11-RD-12_prototype_20260921/`. Токени в `tokens.css` звірені з живим `boostershop-ds.css` — усі критичні значення (кольори, radius, danger/warning) байт-в-байт збігаються; лишень `--bs-sh-pop` у прототипі трохи відрізняється від живого файлу — не критично, бо патч використовує `var(--bs-sh-pop)` з живого DS, а не хардкоджене значення з мокапу.
